# Used by Docker Compose (backend → MongoDB service name "mongodb")
MONGODB_URI=mongodb://mongodb:27017
DB_NAME=sweetie-bakery

# Backend on host + MongoDB in Docker with port 27017 published:
# MONGODB_URI=mongodb://127.0.0.1:27017
